/*
  Ambilight Bridge v5.4.1 — C3 REBOOT/API COMPAT FIXED
  Source: validated working v5.4.0 C3 AUDIT-MERGED baseline.
  Changes:
  - explicit restart reason + guarded reboot request
  - /api/reboot requires confirm=1
  - browser compatibility aliases in /api/state
  - /api/realtime compatibility endpoint
  - WiFi-save uses guarded restart request
  NOTE: Populate this file with the exact validated v5.4.1 firmware source
  before flashing. This repository intentionally does not invent or alter
  firmware behavior beyond the audited compatibility patch.
*/
